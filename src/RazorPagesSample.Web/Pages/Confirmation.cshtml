@page
@model ConfirmationModel
@{
    ViewData["Title"] = "Contact";
}

<div class="container">
    <h1 class="mt-4 mb-3">
        Thank you
    </h1>
    <hr />
    @if (!string.IsNullOrWhiteSpace(Model.Message))
    {
        <h3>@Model.Message</h3>
    }
</div>
